{
  ...
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/${{ github.event.repository.name }}/", 
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  ...
}
